"use client"

import styled from "@mui/system/styled";

const Item = styled("div")(() => ({
  // padding: "10vw",
  borderRadius: "2vw",
  // textAlign: "center",
  aspectRatio: "1/1.25",
  backgroundColor: "white",
}));

export default Item;
